{
  "_comment": "Central platform defaults. Edit values below; keys starting with _ are documentation only.",

  "architecture": "x86_64",
  "_architecture_options": "x86_64 | arm64",

  "backend_seed_image": {
    "ecr_repository_suffix": "_backend",
    "image_tag": "seed",
    "uri_template": "{account}.dkr.ecr.{region}.amazonaws.com/{ecr_repository_name}:{image_tag}"
  },
  "_backend_seed_image_note": "uri_template placeholders: account, region, ecr_repository_name, image_tag. Repository name is {env_name} + ecr_repository_suffix."
}
