{
  "_comment": "Copy this file to customer-config.json and fill in identity fields before running cdk synth. Or run: renglo system init",

  "env_name": "myenv",

  "github_repo": "MyOrg/my-bom-repo",

  "enable_staging": true,

  "email_from": "noreply@your-app-domain.com",
  "email_identity_type": "domain",
  "email_hosted_zone_id": "Z0123456789EXAMPLE",

  "_email_from_note": "Required. SES from-address for team invite email — the primary way users join after the first admin.",
  "_email_identity_type_note": "Required. email = verify via inbox link. domain = verify domain; add email_hosted_zone_id if the public hosted zone is in this AWS account for automatic DKIM records; omit zone id to export DkimRecord* outputs for manual DNS elsewhere.",
  "_email_hosted_zone_id_note": "Required for domain + Route53 (pattern A). Omit when DNS is outside this account (pattern B). Find with: aws route53 list-hosted-zones.",
  "_account_region_note": "Account and region are NOT in this file. Templates are env-agnostic (AWS::AccountId / AWS::Region). Choose the target account/region at deploy time (AWS profile / CLI region).",
  "_platform_defaults_note": "architecture, backend seed image URI/tag, etc. → launcher/cdk/platform_defaults.json",
  "_github_oidc_ids_note": "Optional GitHub owner/repo IDs for immutable OIDC subject claims (repo:ORG@ID/REPO@ID:environment:STAGE). When set, IAM trust policies accept both legacy and immutable sub formats. Find IDs via: gh api repos/ORG/REPO --jq '{owner_id: .owner.id, repo_id: .id}'. Omit when unknown.",

  "package_registry": {
    "domain_owners": []
  },
  "_package_registry_note": "Optional. Same-account CodeArtifact read is always granted — omit package_registry or use domain_owners: [] for internal-only. List foreign publisher AWS account IDs in domain_owners for cross-account pull (each publisher must also list this tenant in reader_aws_accounts)."
}
